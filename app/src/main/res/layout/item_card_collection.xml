<androidx.cardview.widget.CardView
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginVertical="8dp"
        app:cardCornerRadius="21dp"
        app:cardElevation="0dp">

    <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:padding="0dp">


        <ImageView
                android:id="@+id/card_collection_background"
                android:layout_width="match_parent"
                   android:layout_height="match_parent"
                   android:src="@drawable/card_collection_background"
                   android:scaleType="fitXY"
                   app:layout_constraintTop_toTopOf="parent"
                   app:layout_constraintBottom_toBottomOf="parent"
                   app:layout_constraintLeft_toLeftOf="parent"
                   app:layout_constraintRight_toRightOf="parent"/>


        <!-- Иконка привычки -->

        <ImageView
                android:id="@+id/icon_card_collection"
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:src="@drawable/icon_card_collection"
                app:layout_constraintTop_toTopOf="@+id/card_collection_background"
                app:layout_constraintStart_toStartOf="@+id/card_collection_background"
                android:layout_marginTop="16dp"
                android:layout_marginStart="16dp"
                app:tint="@android:color/black"/>

        <!-- Иконка привычки -->

        <ImageView
                android:id="@+id/icon_card_info_collection"
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:src="@drawable/icon_card_info_collection"
                app:layout_constraintTop_toTopOf="@+id/card_collection_background"
                app:layout_constraintEnd_toEndOf="@+id/card_collection_background"
                android:layout_marginTop="16dp"
                android:layout_marginEnd="16dp"
                app:tint="@android:color/black"/>

        <!-- Название привычки -->

        <TextView
                android:id="@+id/card_collection_title"
                android:layout_width="142dp"
                android:layout_height="wrap_content"
                android:text="Коллеккция 1"
                android:fontFamily="@font/gilroy_bold"
                android:textSize="16sp"
                android:textStyle="bold"
                android:textColor="@android:color/black"
                app:layout_constraintLeft_toLeftOf="parent"
                app:layout_constraintBottom_toBottomOf="@+id/card_collection_background"
                android:layout_marginBottom="16dp"
                android:layout_marginStart="15dp"
        />

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.cardview.widget.CardView>
